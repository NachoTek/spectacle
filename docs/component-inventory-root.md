# Component Inventory (root)

## UI Components

See `docs/ui-component-inventory-root.md` for QML, .ui, and selected C++ UI classes.

## Core Components

- SpectacleCore (core capture/recording logic)
- ExportManager (save/export workflows)
- OcrManager (OCR integration)
- Platform backends (KWin/Wayland/XCB) in `src/Platforms/`